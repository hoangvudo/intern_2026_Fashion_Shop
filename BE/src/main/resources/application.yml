spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fashion_shop
    username: root
    password: 

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: vanbenha123@gmail.comn
    password: kebe jcks whag txqb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: fashionshop-super-secret-key-must-be-at-least-32-characters-long
  access-token-expiration: 900000
  refresh-token-expiration: 604800000

app:
  frontend-url: http://localhost:3000

# ✅ Giữ nguyên block này cho @Value trong OAuth2Service
oauth2:
  google:
    client-id: ${GOOGLE_OAUTH_CLIENT_ID}
    client-secret: ${GOOGLE_OAUTH_CLIENT_SECRET}
    redirect-uri: http://localhost:8080/api/auth/oauth2/google/callback
  facebook:
    client-id: ${FACEBOOK_OAUTH_CLIENT_ID}
    client-secret: ${FACEBOOK_OAUTH_CLIENT_SECRET}
    redirect-uri: http://localhost:8080/api/auth/oauth2/facebook/callback

spring.application.name: backend