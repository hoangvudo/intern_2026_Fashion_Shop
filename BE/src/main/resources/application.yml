spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fashion_shop
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: vanbenha123@gmail.com
    password: kebe jcks whag txqb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
# JWT
jwt:
  secret: fashionshop-super-secret-key-must-be-at-least-32-characters-long
  access-token-expiration: 900000
  refresh-token-expiration: 604800000

# Frontend URL (dùng trong link email reset mật khẩu)
app:
  frontend-url: http://localhost:3000

# OAuth2 Google
oauth2:
  google:
    client-id: YOUR_GOOGLE_CLIENT_ID
    client-secret: YOUR_GOOGLE_CLIENT_SECRET
    redirect-uri: http://localhost:8080/api/auth/oauth2/google/callback

spring.application.name: backend